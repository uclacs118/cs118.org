# Spring 2026 archive

The original Spring 2026 content of cs118.org has been moved here so it no
longer renders on the live site (which now shows a placeholder at `/`).

- `pages/` — mirror of the original `src/pages/` tree
- `public/ctfs/` — CTF diagrams
- `src-assets/projects/` — project diagrams

To restore: `mv src/_archive/s26/pages/* src/pages/` (and the assets back),
or `git revert` the archive commit. Git history is the canonical backup.
